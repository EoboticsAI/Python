#Version 1 : Using math module

# Program to compute the factorial of a given number using math module

"""
Returns the factorial of a given number `n` using the built-in math module.

Parameters:
n (int): The number to compute the factorial of. Should be a non-negative integer.

Returns:
int: The factorial of `n` (i.e., n!).
"""

import math

def find_factorial(n):
    
    return math.factorial(n)

num = int(input("Enter the Number : "))

print(f"Factorial of {num}: {find_factorial(num)}")



#Version 2 : Using a custom loop (no math module)

# Program to compute the factorial of a given number using a custom loop

"""
Returns the factorial of a given number `n` without using any external libraries.

Parameters:
n (int): The number to compute the factorial of. Should be a non-negative integer.

Returns:
int: The factorial of `n` (i.e., n!).
"""

def find_factorial(n):
    
    result = 1
    for i in range(2, n + 1):
        result *= i  # Multiply result by the current number i
    return result
    
num = int(input("Enter the Number : "))
print(f"Factorial of {num}: {find_factorial(num)}")





